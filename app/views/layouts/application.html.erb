<!DOCTYPE html>
<html>
<head>
  <title>BeverageTracker</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
      <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <%= stylesheet_pack_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 text-gray-800 antialiased" style="text-size-adjust: 100%;">
  <!-- Responsive Navbar -->
  <nav class="bg-gradient-to-r from-red-400 to-red-500 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <!-- Logo -->
        <div class="flex-shrink-0">
          <%= link_to "BeverageTracker", root_path, class: "text-white text-xl font-bold tracking-tight" %>
        </div>

        <!-- Hamburger Menu (Mobile) -->
        <div class="md:hidden">
          <button id="menu-toggle" class="text-white focus:outline-none">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
            </svg>
          </button>
        </div>

        <!-- Menu Items (Desktop) -->
        <div class="hidden md:flex space-x-6">
          <%= link_to "Home", root_path, class: "text-white hover:text-indigo-200 transition duration-200" %>
          <%= link_to "Beverages", beverages_path, class: "text-white hover:text-indigo-200 transition duration-200" %>
          <!-- Add more links as needed -->
        </div>
      </div>

      <!-- Mobile Menu (Hidden by Default) -->
      <div class="hidden md:hidden" id="mobile-menu">
        <div class="px-2 pt-2 pb-3 space-y-1 bg-indigo-700">
          <%= link_to "Home", root_path, class: "block text-white hover:bg-indigo-600 px-3 py-2 rounded-md" %>
          <%= link_to "Beverages", beverages_path, class: "block text-white hover:bg-indigo-600 px-3 py-2 rounded-md" %>
          <!-- Add more links as needed -->
        </div>
      </div>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="min-h-screen">
    <%= yield %>
  </main>

  <!-- Footer (Optional) -->
  <footer class="bg-gray-800 text-white text-center py-4 mt-8">
    <p>&copy; <%= Time.current.year %> BeverageTracker. All rights reserved.</p>
  </footer>
</body>
</html>